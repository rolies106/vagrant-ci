---
apt_pin_dir: "/etc/apt/preferences.d"
apt_source_state: "{{ item.state | default('present') }}"

apt_source_host: []
apt_source_group: []
apt_source_all: []

apt_pin_host: []
apt_pin_group: []
apt_pin_all: []
